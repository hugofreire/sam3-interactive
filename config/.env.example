# SAM3 Configuration
# Copy this file to .env and adjust values for your environment

# === Server ===
PORT=3001

# === GPU Configuration ===
# GPU device index for SAM3 and training
# Set to '' (empty) for CPU-only mode (Raspberry Pi)
CUDA_VISIBLE_DEVICES=1

# === Training ===
# GPU device for YOLO training ('cpu' for Raspberry Pi)
TRAINING_DEVICE=1
# Number of data loader workers (0 for Pi to avoid memory issues)
TRAINING_WORKERS=4

# === Frontend (optional, for remote backend access) ===
# VITE_API_URL=http://localhost:3001/api
